#a,href denotes as a link
# while checking elements we can go to console and check wether it exists more than 1times,so can save time
# to run 
# in console type $$('a') and enter we can sell all a tags.(this is called css selector) and for to run
# on python type driver.find_element_by_css_selector()
#$x: for x path x path starts with // and @before what we are selecting like id,class, or etc
#use id is the best